import 'package:flutter_test/flutter_test.dart';
import 'package:network/model/news/news.dart';
import 'package:space/pages/home/home_viewmodel.dart';

void main() {
  /*
  group('Home Test', () {
    var homeProvider = HomeProvider();
    var item = News(id: 1);
    test('Haber Ekleme', () {
      homeProvider.setNewsList([item]);
      expect(homeProvider.newsList.contains(item), true);
    });
    test('Haber Çıkarma', () {
      homeProvider.removeNews(item);
      expect(homeProvider.newsList.contains(item), false);
    });
  });*/
}
